# Task-add
A simple Task Manager web application built using a client-server architecture. The frontend is developed using HTML, CSS, and JavaScript, while the backend is powered by Node.js and Express (server.js). Users can add and delete tasks dynamically, with the server handling API requests for task management.
